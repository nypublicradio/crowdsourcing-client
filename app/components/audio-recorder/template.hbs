<div class="audio-recorder__header">
  <p>
    Press the record button and begin:
  </p>
</div>

<div class="audio-recorder__sticky">
  {{#action-button
    disabled=connect.isRunning
    click=(action toggleRecord)
    class=(concat 'gtm__click-tracking audio-recorder__button' (if record.isRunning ' is-recording'))
    data-action=(if record.isRunning 'Stop Record' 'Start Record')
    data-label=(concat title '|' id)}}
    {{#if twilio.connect.isRunning}}
      Connecting...
    {{else if record.isRunning}}
      <span class="stop-icon"></span> Stop Recording
    {{else}}
      <span class="record-icon"></span> Start Recording
    {{/if}}
  {{/action-button}}
</div>


{{#if question.questionText}}
  <p class="audio-recorder__script">
    {{{question.questionText}}}
  </p>
{{/if}}
